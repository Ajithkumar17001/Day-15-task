let count = 10;

setTimeout(() => {
    count--;
    document.querySelector('b').innerHTML = count;
    setTimeout(() => {
        count--;
        document.querySelector('b').innerHTML = count;
        setTimeout(() => {
            count--;
            document.querySelector('b').innerHTML = count;
            setTimeout(() => {
                count--;
                document.querySelector('b').innerHTML = count;
                setTimeout(() => {
                    count--;
                    document.querySelector('b').innerHTML = count;
                    setTimeout(() => {
                        count--;
                        document.querySelector('b').innerHTML = count;
                        setTimeout(() => {
                            count--;
                            document.querySelector('b').innerHTML = count;
                            setTimeout(() => {
                                count--;
                                document.querySelector('b').innerHTML = count;
                                setTimeout(() => {
                                    count--;
                                    document.querySelector('b').innerHTML = count;
                                    setTimeout(() => {
                                        count = "Happy Independence Day"
                                        document.querySelector('b').innerHTML = count;
                                    }, 1000);
                                }, 1000);
                            }, 1000);
                        }, 1000);
                    }, 1000);
                }, 1000);
            }, 1000);
        }, 1000);
    }, 1000);
}, 1000);